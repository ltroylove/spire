# Claude Code — Lessons Learned

---

## Project Overview

Spire is a web-based EQEmu server management and content editing tool.

- **Our fork:** `ltroylove/spire` — active base is **Valorith/spire** (not EQEmu/spire, which went stale Oct 2025)
- **Local clone:** `C:\Projects\spire`
- **Stack:** Go 1.26 backend (Echo + GORM) · TypeScript/Vue 2 frontend (Vue CLI) · Docker MySQL (MariaDB)
- **Ports:** Backend `localhost:3001` · Frontend `localhost:8080` · MySQL Docker `localhost:33066`

---

## Windows Dev Server Startup

The `.bat` files (`windows-backend-web.bat`, `windows-frontend-web-dev.bat`) are the documented approach but **do not work from the Claude Code bash shell**. Use these commands instead:

**Start MySQL (if not running):**
```
docker-compose up -d mysql
```

**Build & start backend** (Go must be on PATH — it's at `C:\Program Files\Go\bin`):
```bash
export PATH="$PATH:/c/Program Files/Go/bin:/c/Users/Dad/go/bin"
go build -o spire.exe . && ./spire.exe http:serve --port=3001 null
```

**Start frontend:**
```bash
cd frontend
NODE_OPTIONS=--openssl-legacy-provider node --max_old_space_size=4096 --stack-size=10000 \
  node_modules/@vue/cli-service/bin/vue-cli-service.js serve --host=0.0.0.0
```

**Prerequisites that must exist:**
- `frontend/node_modules/` — run `npm install` inside `frontend/` if missing
- `frontend/.env` with `VUE_APP_BACKEND_BASE_URL=http://localhost:3001` — already present; source is `frontend/.env.example.windows`
- `.env` copied from `.env.dev` — already present

**air hot-reload note:** `.air.windows.toml` references `windows-build-spire.bat` which does not exist (real file is `windows-build-spire.exe.bat`). Air is broken on Windows host; use `go build` + run directly instead.

---

## Backend Architecture

### Dependency Injection — Google Wire

All wiring lives in `boot/`. When adding a new controller or service:

1. Add the constructor to the appropriate `wire.NewSet` in `boot/inject_http.go` (or `inject_services.go` / `inject_commands.go`)
2. Add it to `provideControllers()` in `boot/inject_http.go` and assign to the right controller group:
   - `v1controllersNoAuth` — public endpoints (no login required); has rate limiter + gzip
   - `v1controllersNoPermissions` — authenticated but no ACL check
   - `v1controllers` — authenticated + permissions ACL
   - `authControllers` — auth routes (under `/auth/`)
3. **Do not edit `boot/wire_gen.go`** — it is regenerated by `wire` and will be overwritten

### Auto-generated files — never edit manually

| Path | Generator |
|------|-----------|
| `internal/http/crudcontrollers/` | `make generate` (db schema → CRUD) |
| `frontend/src/app/api/` | `make generate-swagger` then Swagger codegen |
| `docs/swagger.json` · `docs/swagger.yaml` | `make generate-swagger` (swaggo) |
| `boot/wire_gen.go` | `wire` (Google Wire) |

### Hand-written — safe to contribute to

- `internal/http/controllers/` — business logic controllers
- `internal/http/middleware/` — Echo middleware
- `internal/questapi/` — Quest API explorer
- `internal/eqemuserver/` — installer, config, launcher
- `frontend/src/app/*.ts` — helpers (loot.ts, merchants.ts, spells.ts, etc.)
- `frontend/src/views/` — Vue editor pages
- `frontend/src/components/` — shared Vue components

---

## Contributing

- **All PRs target Valorith/spire** — never EQEmu/spire unless that changes
- Remote `valorith` is fetch-only (`no_push`); our pushes go to `origin` (ltroylove/spire)
- Sync Valorith updates: `git fetch valorith && git merge valorith/master`

### Release workflow

Use the `/dev/spirechangelog` tool from a live repo checkout to manage releases:
- Bumps `package.json` version
- Scaffolds the next section in `CHANGELOG.md` (source of truth for release notes)
- Generates a draft from recent commits
- On merge to `master`, Drone CI publishes to GitHub Releases automatically

Release repo resolution order: `SPIRE_RELEASE_REPO` env → `package.json.repository.url` → `git remote upstream` → `git remote origin` → fallback `EQEmuTools/spire`

---

## Env / Config Notes

- `APP_ENV=local` (or `dev`/`desktop`) → `IsAppEnvLocal()` returns true; skips symlink creation, skips webhook HMAC verification, etc.
- `APP_ENV=production` → enables webhook verification, read-only hosted mode, etc.
- Running on Windows host outside Docker → `.env.debug.host` is auto-loaded (overrides MySQL host to `localhost:33066`)
- `QUEST_API_WEBHOOK_SECRET` — GitHub webhook HMAC secret; leave empty in dev (verification skipped locally)

---



## EQ Progress Bar / Cast Bar

### How the EQ progress bar works visually

`EQProgressBar.vue` renders two layers:

1. **Outer div** (`.eq-progress-bar`) — the track background
   - `background-image: url(progress_bar_bottom.png)` — **almost entirely transparent** (only a few dark pixels at the edges/bottom tick marks). It relies on the EQ window's `super_fancy_bg.png` texture showing through to create a visible track.
2. **Inner div** (fill) — solid `background-color` that animates `width: 0% → 100%`
   - `::before` pseudo-element — `progress_bar_top.png` glass overlay (border + tick marks), sized to `calc(fill-width + 8px)`, positioned at `-4px` left / `-2px` top relative to the fill div.

**Critical**: `progress_bar_top.png` is attached to the **fill div**, so it always moves with the fill. This is by design when the bar is inside a visible EQ window (the static track texture provides a visual anchor). When the containing background blocks the EQ texture, the moving overlay is the only visible element and looks broken.

### The "border moves with the bar" bug

**Symptoms**: The decorative border and tick marks shrink/move as the fill drains instead of staying at full bar width.

**Root cause**: `progress_bar_top.png` (the `::before`) is scoped to the fill div — it can never be stationary through CSS alone on the container. Every attempt to fix this via parent container CSS (wrappers, `position: absolute` on the fill, `background-color` overrides) was treating a symptom.

**Correct fix** (in `LoaderCastBarTimer.vue`):
- Wrap `<eq-progress-bar>` in a `position: relative` container
- Add a sibling `div.cast-bar-frame` at `z-index: 2` with `progress_bar_top.png` at full width (`width: calc(100% + 8px)`, `background-size: 100% 100%`)
- Suppress the fill's moving `::before` via `::v-deep .eq-progress-bar > div::before { display: none }`

This is the only fix that addresses the root cause.

### Why background-color fixes appeared to have "no effect"

The EQ window's `super_fancy_bg.png` texture normally shows through the nearly-transparent track background. When a containing element (e.g. a rank card with `background: rgba(18,31,53,0.35)`) blocks that texture, the track becomes invisible — but this was a **symptom**, not the root cause of the border moving.

### Vue 2 scoped CSS and deep component chains

- A scoped rule like `.parent .eq-progress-bar { ... }` becomes `.parent[data-v-xxx] .eq-progress-bar[data-v-xxx]`. For two levels of component nesting (AaEditor → LoaderCastBarTimer → EQProgressBar), the `data-v-xxx` propagation may not reach the final DOM root reliably.
- Use `::v-deep` (e.g. `.parent ::v-deep .eq-progress-bar`) to avoid this.
- Use **inline style on the component element** (`<loader-cast-bar-timer style="...">`) as the most reliable fallback — Vue 2 merges it through the component chain onto the root DOM element.

### Diagnosis approach that works

1. **Read the actual asset files** before theorising. Running pixel analysis on `progress_bar_bottom.png` immediately revealed it was nearly transparent — hours of CSS debugging could have been skipped.
2. **Look at working examples first**. ItemEditor and SpellEditor had working cast bars. The structural difference (no blocking container background, EQ window texture visible) explained everything.
3. **Distinguish visual bugs from layout bugs**. "The border moves with the bar" looked like a layout/width bug but was actually a structural CSS issue — the overlay element was attached to the wrong DOM node.
4. **When CSS fixes don't work after 2 attempts, question the diagnosis** — don't keep trying variations of the same approach.
